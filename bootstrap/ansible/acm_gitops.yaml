---
- name: Playbook to integrate gitops with RHACM
  hosts: localhost
  connection: local
  tasks:
    - name: Configure managedclustersetbinding
      kubernetes.core.k8s:
        state: present
        src: ../rhacm-gitops/01_managedclustersetbinding.yaml 

    - name: Configure Placement
      kubernetes.core.k8s:
        state: present
        src: ../rhacm-gitops/02_placement.yaml 

    - name: Configure gitopscluster
      kubernetes.core.k8s:
        state: present
        src: ../rhacm-gitops/03_gitopscluster.yaml 
     
   
